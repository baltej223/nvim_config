# Neovim Config
<img width="1920" height="1145" alt="image" src="https://github.com/user-attachments/assets/5b3bc374-2f4c-4f88-97d4-993e01fa9059" />
## Requirements

- Neovim
- git
- ripgrep
- fd

## Installation

```
git clone https://github.com/baltej223/nvim_config ~/.config/nvim
```

or ssh one, if you prefer that,

```
git clone git@github.com:baltej223/nvim_config.git ~/.config/nvim
```

## Keybindings

- `<leader>` -> space key
- `<leader><leader>` -> find files
- `<leader>bd` -> Close Current Buffer

## Plugins

- lazy.nvim
- telescope.nvim
- treesitter

Note: I genuinely have no idea why there are 13 contributors where as I have only contributer, I guess the first commit which had the original git file had these commit with them, and even after Fixup (force push) these contributors refuse to go.
